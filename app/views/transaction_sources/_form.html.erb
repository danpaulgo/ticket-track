<%= form_with(model: transaction_source, local: true) do |form| %>
  <% if transaction_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction_source.errors.count, "error") %> prohibited this transaction_source from being saved:</h2>

      <ul>
      <% transaction_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
